# Raw Locust CSV output is regenerated per run and not committed - it's
# bulky and each run overwrites/adds new files. The PNGs (generated by
# generate_graphs.py) and ws_ladder.jsonl ARE committed: they're the
# actual artifacts REPORT.md references and are small/stable enough to
# keep as supporting evidence for that report.
*_stats.csv
*_stats_history.csv
*_failures.csv
*_exceptions.csv
