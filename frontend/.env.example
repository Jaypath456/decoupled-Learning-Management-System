# .env.example
REACT_APP_API_URL=http://localhost:8000/api

# Optional: base URL for WebSocket connections (see src/api/ws.js).
# If unset, it's derived from REACT_APP_API_URL (http->ws, strip /api).
# REACT_APP_WS_URL=ws://localhost:8000